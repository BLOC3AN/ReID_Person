# Person ReID System - UI Service (Streamlit)
FROM python:3.10-slim

# Set environment variables
ENV PYTHONUNBUFFERED=1 \
    PYTHONDONTWRITEBYTECODE=1 \
    PIP_NO_CACHE_DIR=1

# Install system dependencies (minimal)
RUN apt-get update && apt-get install -y \
    curl \
    && rm -rf /var/lib/apt/lists/*

# Set working directory
WORKDIR /app

# Copy UI-specific requirements
COPY requirements.ui.txt requirements.txt

# Install Python dependencies
RUN pip install --upgrade pip && \
    pip install -r requirements.txt

# Copy only necessary files for UI
COPY app.py .
COPY configs/ configs/
COPY utils/ utils/

# Create necessary directories
RUN mkdir -p /app/outputs/videos \
    /app/outputs/csv \
    /app/outputs/logs \
    /app/outputs/extracted_objects \
    /app/data/videos \
    /app/data/database \
    /app/logs

# Expose Streamlit port
EXPOSE 8501

# Health check
HEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \
    CMD curl -f http://localhost:8501/_stcore/health || exit 1

# Run Streamlit UI
CMD ["streamlit", "run", "app.py", "--server.port=8501", "--server.address=0.0.0.0"]

